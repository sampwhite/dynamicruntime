package org.dynamicruntime.schemadef;

import org.dynamicruntime.logging.AppLogger;

@SuppressWarnings("WeakerAccess")
public class LogSchema {
    public static final AppLogger log = new AppLogger(LogSchema.class);
}
